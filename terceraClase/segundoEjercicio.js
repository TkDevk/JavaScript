let a = parseInt(prompt("Ingrese un numero"));
let b = parseInt(prompt("Ingrese un numero"));

let operaciones= {
    suma: a+b,
    resta: a-b,
    multiplicacion: a*b,
    division: a/b

}

alert("La suma es : " + operaciones.suma + "La resta es : " + operaciones.suma );
